--START OF REQUIREMENTS--

#run these commands in the terminal, they work cross platform
curl -fsSL https://ollama.com/install.sh | sh
ollama run gemma3:1b

#Create a venv for python and use the pip install for ollama
pip install -r 'requirements.txt'

--END OF REQUIREMENTS--